spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:jampick_test}?serverTimezone=UTC&characterEncoding=UTF-8
    password: ${MYSQL_ROOT_PASSWORD:mysql}